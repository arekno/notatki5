Zadanie 1
Stwórz wykres liniowy, który wyœwietli liczbê urodzonych dzieci dla ka¿dego roku.

xlsx = pd.ExcelFile('imiona.xlsx')
df = pd.read_excel(xlsx, header=0)
print(df)
# zad1
# roczniki = df['Rok'].unique()
# grupa = df.groupby(['Rok']).agg({'Liczba':['sum']})
# wykres = grupa.plot()
# wykres.set_ylabel('Liczba urodzonych dzieci')
# wykres.set_xticks(roczniki)
# wykres.tick_params(axis='x', labelrotation=40)
# wykres.legend()
# plt.subplots_adjust(left=0.15, right=0.9, bottom=0.15, top=0.9)
# plt.title("Liczba urodzonych dzieci dla ka¿dego roku")
# plt.show()

Stwórz wykres s³upkowy, który wyœwietli liczbê urodzonych ch³opców i dziewczynek z ca³ego zbioru.# #zad2
# grupa = df.groupby(['Plec']).agg({'Liczba':['sum']})
# wykres = grupa.plot.bar(ylabel='Liczba urodzeñ')
# wykres.legend()
# plt.xticks(rotation=0)
# plt.title("Liczba urodzonych ch³opców i dziewczynek")
# plt.show()


Wykres ko³owy z wartoœciami % ukazuj¹cymi iloœæ urodzonych ch³opców i dziewczynek w ostatnich 5
latach z datasetu.#zad3
# grupa = df[df['Rok'] > 2012].groupby(['Plec']).agg({'Liczba':['sum']})
# wykres = grupa.plot.pie(subplots=True, autopct='%.2f %%', fontsize=20)
# plt.legend()
# plt.show()


Wyœwietl na pomoc¹ wykresu s³upkowego iloœæ z³o¿onych zamówieñ przez poszczególnych
sprzedawców (zbiór danych zamówienia.csv).
#zad4
df = pd.read_csv('zamowienia.csv', delimiter=';')
policzone = df.groupby('Sprzedawca').size()
policzone.plot.bar()
plt.ylabel("liczba zamówieñ")
plt.subplots_adjust(left=0.1, right=0.9, bottom=0.2, top=0.9)
plt.title('Iloœæ zamówieñ sprzedawców')
plt.show()